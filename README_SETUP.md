# 🚀 Настройка и запуск мультиагентной системы

## 📋 Требования

- Python 3.8+
- Yandex Cloud аккаунт с API ключом

## 🔧 Установка зависимостей

```bash
pip install -r requirements.txt
```

## ⚙️ Настройка YandexGPT

### 1. Создайте файл `.env` в корне проекта:

```bash
# Скопируйте пример
cp config.env.example .env

# Отредактируйте файл .env
YANDEX_FOLDER_ID=ваш_folder_id
YANDEX_API_KEY=ваш_api_key
YANDEX_MODEL=yandexgpt-lite
YANDEX_VERSION=rc
```

### 2. Получите credentials:

1. Зайдите в [Yandex Cloud Console](https://console.cloud.yandex.ru/)
2. Создайте или выберите каталог (folder)
3. Скопируйте ID каталога в `YANDEX_FOLDER_ID`
4. Создайте API ключ в разделе "Сервисные аккаунты"
5. Скопируйте API ключ в `YANDEX_API_KEY`

## 🚀 Запуск

### С YandexGPT:
```bash
python main.py
```

### Без YandexGPT (fallback режим):
```bash
# Просто запустите без .env файла
python main.py
```

## 💬 Примеры использования

```
👤 Вы: привет
🤖 Агент: Привет! Как дела?

👤 Вы: что ты умеешь?
🤖 Агент: Я умею помогать с задачами, календарем и отвечать на вопросы. Что вас интересует?

👤 Вы: помощь
🤖 Агент: Я умею помогать с задачами, календарем и отвечать на вопросы. Что вас интересует?

👤 Вы: пока
🤖 Агент: До свидания! Обращайтесь снова.
```

## 🔍 Режимы работы

### 1. **С YandexGPT** (рекомендуется):
- Умная маршрутизация запросов
- Генерация ответов через LLM
- Обработка сложных запросов


## 🛠️ Структура проекта

```
task_langgraph/
├── main.py                 # Главный файл
├── agent/
│   ├── __init__.py
│   ├── agent.py           # Класс агента
│   └── core/
│       ├── __init__.py
│       ├── enums.py       # Перечисления
│       ├── models.py      # Модели данных
│       ├── nodes.py       # Узлы графа
│       ├── prompts.py     # Промпты для LLM
│       ├── llm.py         # Интеграция с YandexGPT
│       ├── tools.py       # Инструменты
│       └── utils.py       # Утилиты
├── requirements.txt        # Зависимости
└── config.env.example     # Пример конфигурации
```

## 🔧 Устранение неполадок

### Ошибка "YandexGPT request failed":
- Проверьте правильность `YANDEX_FOLDER_ID` и `YANDEX_API_KEY`
- Убедитесь, что у вас есть доступ к YandexGPT
- Проверьте баланс в Yandex Cloud

### Ошибка импорта модулей:
- Убедитесь, что вы в корневой папке проекта
- Проверьте, что все `__init__.py` файлы созданы

### Fallback режим не работает:
- Проверьте логику в `nodes.py`
- Убедитесь, что `self.gpt` равен `None`

## 📝 Дополнительные возможности

- **Расширение промптов**: отредактируйте `agent/core/prompts.py`
- **Добавление узлов**: создайте новые узлы в `agent/core/nodes.py`
- **Интеграция с другими LLM**: замените `YandexGPT` в `agent/core/llm.py`

## 🎯 Следующие шаги

1. Настройте YandexGPT credentials
2. Запустите систему
3. Протестируйте различные типы запросов
4. Расширьте функциональность по необходимости
