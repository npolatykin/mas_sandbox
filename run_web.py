#!/usr/bin/env python3
"""
Простой скрипт для запуска веб-версии UI Testing Agent
"""

import os
import sys
import uvicorn

# Добавляем путь к модулю agent
sys.path.append(os.path.dirname(os.path.abspath(__file__)))

def main():
    """Запуск веб-сервера"""
    print("🚀 Запуск UI Testing Agent Web Server...")
    print("📁 Рабочая директория:", os.getcwd())
    print("🌐 Сервер будет доступен по адресу: http://localhost:8000")
    print("📱 Откройте браузер и перейдите по указанному адресу")
    print("⏹️  Для остановки сервера нажмите Ctrl+C")
    print("-" * 50)
    
    try:
        # Загружаем переменные окружения
        try:
            from dotenv import load_dotenv
            load_dotenv()
            print("✅ Переменные окружения загружены")
        except ImportError:
            print("⚠️  python-dotenv не установлен. Установите: pip install python-dotenv")
        
        # Проверяем наличие статических файлов
        if not os.path.exists("static/index.html"):
            print("❌ Папка static/ не найдена. Убедитесь, что вы находитесь в корневой директории проекта.")
            return
        
        # Запускаем сервер
        uvicorn.run(
            "api_server:app",
            host="0.0.0.0",
            port=8000,
            reload=True,  # Автоперезагрузка при изменении файлов
            log_level="info"
        )
        
    except KeyboardInterrupt:
        print("\n🛑 Сервер остановлен пользователем")
    except Exception as e:
        print(f"❌ Ошибка запуска сервера: {e}")
        print("💡 Убедитесь, что все зависимости установлены:")
        print("   pip install -r requirements.txt")

if __name__ == "__main__":
    main()

